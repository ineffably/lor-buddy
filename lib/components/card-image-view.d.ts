import { LorCard } from '../types/lor-types';
export interface CardImageViewProps {
    card: LorCard;
    scale?: number;
}
export declare const CardImageView: ({ card, scale }: CardImageViewProps) => import("react/jsx-runtime").JSX.Element;
